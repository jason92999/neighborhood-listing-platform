import { properties } from "@/data/properties"; import PropertyCard from "@/components/PropertyCard"; export default function LabPage() { return (<main className="p-8 max-w-5xl mx-auto"><h1 className="text-2xl font-bold text-gray-900">Neighborhood Property Listing - Lab Space</h1><p className="mt-1 text-gray-500">Workspace for testing property components.</p><div className="grid grid-cols-1 md:grid-cols-2 gap-6 mt-6">{properties.map((p) => (<PropertyCard key={p.id} property={p} />))}</div></main>); }
